// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from "./client";
import { client } from "./client.gen";
import type {
  AdminDeleteAttachmentData,
  AdminDeleteAttachmentErrors,
  AdminDeleteAttachmentResponses,
  AdminDeleteMemoryData,
  AdminDeleteMemoryErrors,
  AdminDeleteMemoryResponses,
  AdminEvictData,
  AdminEvictErrors,
  AdminEvictResponses,
  AdminGetAttachmentContentData,
  AdminGetAttachmentContentErrors,
  AdminGetAttachmentContentResponses,
  AdminGetAttachmentData,
  AdminGetAttachmentDownloadUrlData,
  AdminGetAttachmentDownloadUrlErrors,
  AdminGetAttachmentDownloadUrlResponses,
  AdminGetAttachmentErrors,
  AdminGetAttachmentResponses,
  AdminGetCheckpointData,
  AdminGetCheckpointErrors,
  AdminGetCheckpointResponses,
  AdminGetConversationData,
  AdminGetConversationErrors,
  AdminGetConversationResponses,
  AdminGetEntriesData,
  AdminGetEntriesErrors,
  AdminGetEntriesResponses,
  AdminGetEntryData,
  AdminGetEntryErrors,
  AdminGetEntryResponses,
  AdminGetMembershipsData,
  AdminGetMembershipsErrors,
  AdminGetMembershipsResponses,
  AdminGetMemoryData,
  AdminGetMemoryErrors,
  AdminGetMemoryIndexStatusData,
  AdminGetMemoryIndexStatusErrors,
  AdminGetMemoryIndexStatusResponses,
  AdminGetMemoryPoliciesData,
  AdminGetMemoryPoliciesErrors,
  AdminGetMemoryPoliciesResponses,
  AdminGetMemoryResponses,
  AdminGetMemoryUsageData,
  AdminGetMemoryUsageErrors,
  AdminGetMemoryUsageResponses,
  AdminListAttachmentsData,
  AdminListAttachmentsErrors,
  AdminListAttachmentsResponses,
  AdminListChildConversationsData,
  AdminListChildConversationsErrors,
  AdminListChildConversationsResponses,
  AdminListConversationsData,
  AdminListConversationsErrors,
  AdminListConversationsResponses,
  AdminListForksData,
  AdminListForksErrors,
  AdminListForksResponses,
  AdminListMemoriesData,
  AdminListMemoriesErrors,
  AdminListMemoriesResponses,
  AdminListMemoryNamespacesData,
  AdminListMemoryNamespacesErrors,
  AdminListMemoryNamespacesResponses,
  AdminListTopMemoryUsageData,
  AdminListTopMemoryUsageErrors,
  AdminListTopMemoryUsageResponses,
  AdminPutCheckpointData,
  AdminPutCheckpointErrors,
  AdminPutCheckpointResponses,
  AdminPutMemoryData,
  AdminPutMemoryErrors,
  AdminPutMemoryPoliciesData,
  AdminPutMemoryPoliciesErrors,
  AdminPutMemoryPoliciesResponses,
  AdminPutMemoryResponses,
  AdminSearchConversationsData,
  AdminSearchConversationsErrors,
  AdminSearchConversationsResponses,
  AdminSearchMemoriesData,
  AdminSearchMemoriesErrors,
  AdminSearchMemoriesResponses,
  AdminSubscribeEventsData,
  AdminSubscribeEventsErrors,
  AdminSubscribeEventsResponses,
  AdminTriggerMemoryIndexData,
  AdminTriggerMemoryIndexErrors,
  AdminTriggerMemoryIndexResponses,
  AdminUpdateConversationData,
  AdminUpdateConversationErrors,
  AdminUpdateConversationResponses,
  AdminUpdateMemoryData,
  AdminUpdateMemoryErrors,
  AdminUpdateMemoryResponses,
  GetCacheHitRateData,
  GetCacheHitRateErrors,
  GetCacheHitRateResponses,
  GetDbPoolUtilizationData,
  GetDbPoolUtilizationErrors,
  GetDbPoolUtilizationResponses,
  GetErrorRateData,
  GetErrorRateErrors,
  GetErrorRateResponses,
  GetHealthData,
  GetHealthErrors,
  GetHealthResponses,
  GetLatencyP95Data,
  GetLatencyP95Errors,
  GetLatencyP95Responses,
  GetRequestRateData,
  GetRequestRateErrors,
  GetRequestRateResponses,
  GetStatsSummaryData,
  GetStatsSummaryErrors,
  GetStatsSummaryResponses,
  GetStoreLatencyP95Data,
  GetStoreLatencyP95Errors,
  GetStoreLatencyP95Responses,
  GetStoreThroughputData,
  GetStoreThroughputErrors,
  GetStoreThroughputResponses,
} from "./types.gen";

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = Options2<
  TData,
  ThrowOnError
> & {
  /**
   * You can provide a client instance returned by `createClient()` instead of
   * individual options. This might be also useful if you want to implement a
   * custom client.
   */
  client?: Client;
  /**
   * You can pass arbitrary values through the `meta` object. This can be
   * used to access values that aren't defined as part of the SDK function.
   */
  meta?: Record<string, unknown>;
};

/**
 * Health check
 */
export const getHealth = <ThrowOnError extends boolean = false>(options?: Options<GetHealthData, ThrowOnError>) =>
  (options?.client ?? client).get<GetHealthResponses, GetHealthErrors, ThrowOnError>({ url: "/v1/health", ...options });

/**
 * List episodic memories across users
 *
 * Lists latest memory rows across users for admin/auditor exploration. Does not increment usage counters.
 */
export const adminListMemories = <ThrowOnError extends boolean = false>(
  options?: Options<AdminListMemoriesData, ThrowOnError>,
) =>
  (options?.client ?? client).get<AdminListMemoriesResponses, AdminListMemoriesErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/admin/v1/memories",
    ...options,
  });

/**
 * Update a memory item (admin)
 *
 * Allows admins to update memories across namespaces.
 * Currently supports archiving the active memory item by setting `archived` to `true`.
 * Requires admin role.
 */
export const adminUpdateMemory = <ThrowOnError extends boolean = false>(
  options: Options<AdminUpdateMemoryData, ThrowOnError>,
) =>
  (options.client ?? client).patch<AdminUpdateMemoryResponses, AdminUpdateMemoryErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/admin/v1/memories",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers,
    },
  });

/**
 * Upsert a memory item (admin)
 *
 * Allows admins to write memories across namespaces. Requires admin role.
 */
export const adminPutMemory = <ThrowOnError extends boolean = false>(
  options: Options<AdminPutMemoryData, ThrowOnError>,
) =>
  (options.client ?? client).put<AdminPutMemoryResponses, AdminPutMemoryErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/admin/v1/memories",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers,
    },
  });

/**
 * Search episodic memories across users
 *
 * Searches latest memory rows across users for admin/auditor exploration. Does not increment usage counters.
 */
export const adminSearchMemories = <ThrowOnError extends boolean = false>(
  options: Options<AdminSearchMemoriesData, ThrowOnError>,
) =>
  (options.client ?? client).post<AdminSearchMemoriesResponses, AdminSearchMemoriesErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/admin/v1/memories/search",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers,
    },
  });

/**
 * List episodic memory namespaces across users
 */
export const adminListMemoryNamespaces = <ThrowOnError extends boolean = false>(
  options?: Options<AdminListMemoryNamespacesData, ThrowOnError>,
) =>
  (options?.client ?? client).get<AdminListMemoryNamespacesResponses, AdminListMemoryNamespacesErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/admin/v1/memory-namespaces",
    ...options,
  });

/**
 * Download active episodic memory policy bundle
 *
 * Returns the currently active OPA/Rego policy sources for episodic memory.
 */
export const adminGetMemoryPolicies = <ThrowOnError extends boolean = false>(
  options?: Options<AdminGetMemoryPoliciesData, ThrowOnError>,
) =>
  (options?.client ?? client).get<AdminGetMemoryPoliciesResponses, AdminGetMemoryPoliciesErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/admin/v1/memory-policies",
    ...options,
  });

/**
 * Upload episodic memory policy bundle
 *
 * Replaces active episodic OPA/Rego policies and hot-reloads evaluation without restart.
 */
export const adminPutMemoryPolicies = <ThrowOnError extends boolean = false>(
  options: Options<AdminPutMemoryPoliciesData, ThrowOnError>,
) =>
  (options.client ?? client).put<AdminPutMemoryPoliciesResponses, AdminPutMemoryPoliciesErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/admin/v1/memory-policies",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers,
    },
  });

/**
 * Force-delete an episodic memory by ID
 */
export const adminDeleteMemory = <ThrowOnError extends boolean = false>(
  options: Options<AdminDeleteMemoryData, ThrowOnError>,
) =>
  (options.client ?? client).delete<AdminDeleteMemoryResponses, AdminDeleteMemoryErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/admin/v1/memories/{id}",
    ...options,
  });

/**
 * Get an episodic memory by ID
 */
export const adminGetMemory = <ThrowOnError extends boolean = false>(
  options: Options<AdminGetMemoryData, ThrowOnError>,
) =>
  (options.client ?? client).get<AdminGetMemoryResponses, AdminGetMemoryErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/admin/v1/memories/{id}",
    ...options,
  });

/**
 * Get any entry by ID (admin/auditor)
 *
 * Retrieves a conversation entry by its ID, including entries from archived
 * conversations. Requires auditor or admin role.
 */
export const adminGetEntry = <ThrowOnError extends boolean = false>(
  options: Options<AdminGetEntryData, ThrowOnError>,
) =>
  (options.client ?? client).get<AdminGetEntryResponses, AdminGetEntryErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/entries/{id}",
    ...options,
  });

/**
 * Get episodic memory indexing queue status
 */
export const adminGetMemoryIndexStatus = <ThrowOnError extends boolean = false>(
  options?: Options<AdminGetMemoryIndexStatusData, ThrowOnError>,
) =>
  (options?.client ?? client).get<AdminGetMemoryIndexStatusResponses, AdminGetMemoryIndexStatusErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/admin/v1/memory-index/status",
    ...options,
  });

/**
 * Trigger one episodic memory indexing cycle
 */
export const adminTriggerMemoryIndex = <ThrowOnError extends boolean = false>(
  options?: Options<AdminTriggerMemoryIndexData, ThrowOnError>,
) =>
  (options?.client ?? client).post<AdminTriggerMemoryIndexResponses, AdminTriggerMemoryIndexErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/admin/v1/memory-index/trigger",
    ...options,
  });

/**
 * Get episodic memory usage for a single key
 */
export const adminGetMemoryUsage = <ThrowOnError extends boolean = false>(
  options: Options<AdminGetMemoryUsageData, ThrowOnError>,
) =>
  (options.client ?? client).get<AdminGetMemoryUsageResponses, AdminGetMemoryUsageErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/admin/v1/memory-usage",
    ...options,
  });

/**
 * List top episodic memory usage rows under a namespace prefix
 */
export const adminListTopMemoryUsage = <ThrowOnError extends boolean = false>(
  options?: Options<AdminListTopMemoryUsageData, ThrowOnError>,
) =>
  (options?.client ?? client).get<AdminListTopMemoryUsageResponses, AdminListTopMemoryUsageErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/admin/v1/memory-usage/top",
    ...options,
  });

/**
 * List all conversations (admin/auditor)
 *
 * Lists conversations across all users. Supports filtering by userId,
 * deleted status, and date ranges. Requires auditor or admin role.
 *
 * **Fork Tree Behavior**: Conversations can be forked to create branches. All forks
 * share the same "conversation group" (fork tree). The `mode` parameter controls
 * which conversations from each fork tree are returned.
 */
export const adminListConversations = <ThrowOnError extends boolean = false>(
  options?: Options<AdminListConversationsData, ThrowOnError>,
) =>
  (options?.client ?? client).get<AdminListConversationsResponses, AdminListConversationsErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/conversations",
    ...options,
  });

/**
 * Get any conversation (admin/auditor)
 *
 * Retrieves a conversation by ID, including archived conversations.
 * Requires auditor or admin role.
 */
export const adminGetConversation = <ThrowOnError extends boolean = false>(
  options: Options<AdminGetConversationData, ThrowOnError>,
) =>
  (options.client ?? client).get<AdminGetConversationResponses, AdminGetConversationErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/conversations/{id}",
    ...options,
  });

/**
 * Update any conversation (admin only)
 *
 * Updates admin-managed conversation state. Requires admin role.
 * Setting `archived` to `true` archives the whole fork tree. Setting it to
 * `false` unarchives the fork tree.
 */
export const adminUpdateConversation = <ThrowOnError extends boolean = false>(
  options: Options<AdminUpdateConversationData, ThrowOnError>,
) =>
  (options.client ?? client).patch<AdminUpdateConversationResponses, AdminUpdateConversationErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/conversations/{id}",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers,
    },
  });

/**
 * Get entries from any conversation (admin/auditor)
 *
 * Retrieves entries from a conversation, including archived
 * conversations. Requires auditor or admin role.
 */
export const adminGetEntries = <ThrowOnError extends boolean = false>(
  options: Options<AdminGetEntriesData, ThrowOnError>,
) =>
  (options.client ?? client).get<AdminGetEntriesResponses, AdminGetEntriesErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/conversations/{id}/entries",
    ...options,
  });

/**
 * Get memberships for any conversation (admin/auditor)
 *
 * Retrieves memberships for a conversation. Requires auditor or admin role.
 */
export const adminGetMemberships = <ThrowOnError extends boolean = false>(
  options: Options<AdminGetMembershipsData, ThrowOnError>,
) =>
  (options.client ?? client).get<AdminGetMembershipsResponses, AdminGetMembershipsErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/conversations/{id}/memberships",
    ...options,
  });

/**
 * Get fork navigation for any conversation (admin/auditor)
 *
 * Returns the complete fork-navigation snapshot for the requested conversation:
 * all conversation ids in its fork group and the alternatives at each fork point
 * visible along the requested conversation's ancestry path.
 *
 * This endpoint provides admin access to fork information for any conversation,
 * regardless of ownership. Requires auditor or admin role.
 */
export const adminListForks = <ThrowOnError extends boolean = false>(
  options: Options<AdminListForksData, ThrowOnError>,
) =>
  (options.client ?? client).get<AdminListForksResponses, AdminListForksErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/conversations/{id}/forks",
    ...options,
  });

/**
 * List direct child conversations
 */
export const adminListChildConversations = <ThrowOnError extends boolean = false>(
  options: Options<AdminListChildConversationsData, ThrowOnError>,
) =>
  (options.client ?? client).get<AdminListChildConversationsResponses, AdminListChildConversationsErrors, ThrowOnError>(
    {
      security: [{ scheme: "bearer", type: "http" }],
      url: "/v1/admin/conversations/{id}/children",
      ...options,
    },
  );

/**
 * System-wide semantic search (admin/auditor)
 *
 * Performs semantic search across all conversations, optionally filtered
 * by userId. Requires auditor or admin role.
 */
export const adminSearchConversations = <ThrowOnError extends boolean = false>(
  options: Options<AdminSearchConversationsData, ThrowOnError>,
) =>
  (options.client ?? client).post<AdminSearchConversationsResponses, AdminSearchConversationsErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/conversations/search",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers,
    },
  });

/**
 * Hard-delete archived resources past retention period
 *
 * Permanently removes resources that have been archived for longer than
 * the specified retention period. This operation is irreversible.
 *
 * The operation runs in batches to minimize database lock contention.
 *
 * **Response modes:**
 * - **Synchronous** (default): Blocks until eviction completes, returns 204 No Content.
 * - **SSE streaming** (`?async=true` or `Accept: text/event-stream`): Streams progress
 * updates as Server-Sent Events until completion. Each event contains `{"progress": N}`
 * where N is 0-100. On error, emits `event: error` with `{"error": "message"}`.
 *
 * Requires admin role.
 */
export const adminEvict = <ThrowOnError extends boolean = false>(options: Options<AdminEvictData, ThrowOnError>) =>
  (options.client ?? client).sse.post<AdminEvictResponses, AdminEvictErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/evict",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers,
    },
  });

/**
 * Subscribe to all real-time events (admin SSE)
 *
 * Admin/auditor SSE stream that delivers all events from all users,
 * bypassing membership filtering. Requires admin or auditor role.
 * The optional justification query parameter is logged for audit use
 * when provided, and may be required when admin justification
 * enforcement is enabled on the server.
 */
export const adminSubscribeEvents = <ThrowOnError extends boolean = false>(
  options?: Options<AdminSubscribeEventsData, ThrowOnError>,
) =>
  (options?.client ?? client).sse.get<AdminSubscribeEventsResponses, AdminSubscribeEventsErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/events",
    ...options,
  });

/**
 * Get an admin client checkpoint
 *
 * Returns the checkpoint for an admin client. When the caller has an authenticated client ID, it must match the path clientId.
 */
export const adminGetCheckpoint = <ThrowOnError extends boolean = false>(
  options: Options<AdminGetCheckpointData, ThrowOnError>,
) =>
  (options.client ?? client).get<AdminGetCheckpointResponses, AdminGetCheckpointErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/checkpoints/{clientId}",
    ...options,
  });

/**
 * Upsert an admin client checkpoint
 *
 * Persists an encrypted JSON checkpoint for an admin client. When the caller has an authenticated client ID, it must match the path clientId.
 */
export const adminPutCheckpoint = <ThrowOnError extends boolean = false>(
  options: Options<AdminPutCheckpointData, ThrowOnError>,
) =>
  (options.client ?? client).put<AdminPutCheckpointResponses, AdminPutCheckpointErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/checkpoints/{clientId}",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers,
    },
  });

/**
 * List all attachments across users (admin/auditor)
 *
 * Lists attachments across all users with filtering and pagination.
 * Requires auditor or admin role.
 */
export const adminListAttachments = <ThrowOnError extends boolean = false>(
  options?: Options<AdminListAttachmentsData, ThrowOnError>,
) =>
  (options?.client ?? client).get<AdminListAttachmentsResponses, AdminListAttachmentsErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/attachments",
    ...options,
  });

/**
 * Delete any attachment (admin only)
 *
 * Deletes any attachment regardless of ownership or link status.
 * Uses reference-counting for shared storage keys.
 * Requires admin role.
 */
export const adminDeleteAttachment = <ThrowOnError extends boolean = false>(
  options: Options<AdminDeleteAttachmentData, ThrowOnError>,
) =>
  (options.client ?? client).delete<AdminDeleteAttachmentResponses, AdminDeleteAttachmentErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/attachments/{id}",
    ...options,
    headers: {
      "Content-Type": "application/json",
      ...options.headers,
    },
  });

/**
 * Get attachment metadata (admin/auditor)
 *
 * Returns metadata for any attachment, including archived records.
 * Requires auditor or admin role.
 */
export const adminGetAttachment = <ThrowOnError extends boolean = false>(
  options: Options<AdminGetAttachmentData, ThrowOnError>,
) =>
  (options.client ?? client).get<AdminGetAttachmentResponses, AdminGetAttachmentErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/attachments/{id}",
    ...options,
  });

/**
 * Download attachment content (admin/auditor)
 *
 * Streams the binary content of any attachment, bypassing ownership checks.
 * For S3-backed storage, returns a 302 redirect to a signed URL.
 * Requires auditor or admin role.
 */
export const adminGetAttachmentContent = <ThrowOnError extends boolean = false>(
  options: Options<AdminGetAttachmentContentData, ThrowOnError>,
) =>
  (options.client ?? client).get<AdminGetAttachmentContentResponses, AdminGetAttachmentContentErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/attachments/{id}/content",
    ...options,
  });

/**
 * Get signed download URL for any attachment (admin/auditor)
 *
 * Returns a time-limited signed URL for downloading any attachment
 * without authentication. Requires auditor or admin role.
 */
export const adminGetAttachmentDownloadUrl = <ThrowOnError extends boolean = false>(
  options: Options<AdminGetAttachmentDownloadUrlData, ThrowOnError>,
) =>
  (options.client ?? client).get<
    AdminGetAttachmentDownloadUrlResponses,
    AdminGetAttachmentDownloadUrlErrors,
    ThrowOnError
  >({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/attachments/{id}/download-url",
    ...options,
  });

/**
 * Get HTTP request rate over time
 *
 * Returns the HTTP request rate (requests per second) as a time series.
 * Requires auditor or admin role.
 *
 * **Requires Prometheus**: This endpoint queries Prometheus for metrics.
 * If Prometheus is not configured, returns 501 Not Implemented.
 */
export const getRequestRate = <ThrowOnError extends boolean = false>(
  options?: Options<GetRequestRateData, ThrowOnError>,
) =>
  (options?.client ?? client).get<GetRequestRateResponses, GetRequestRateErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/stats/request-rate",
    ...options,
  });

/**
 * Get 5xx error rate percentage over time
 *
 * Returns the percentage of HTTP requests resulting in 5xx errors as a time series.
 * Requires auditor or admin role.
 *
 * **Requires Prometheus**: This endpoint queries Prometheus for metrics.
 * If Prometheus is not configured, returns 501 Not Implemented.
 */
export const getErrorRate = <ThrowOnError extends boolean = false>(options?: Options<GetErrorRateData, ThrowOnError>) =>
  (options?.client ?? client).get<GetErrorRateResponses, GetErrorRateErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/stats/error-rate",
    ...options,
  });

/**
 * Get P95 response latency over time
 *
 * Returns the 95th percentile HTTP response latency in seconds as a time series.
 * Requires auditor or admin role.
 *
 * **Requires Prometheus**: This endpoint queries Prometheus for metrics.
 * If Prometheus is not configured, returns 501 Not Implemented.
 */
export const getLatencyP95 = <ThrowOnError extends boolean = false>(
  options?: Options<GetLatencyP95Data, ThrowOnError>,
) =>
  (options?.client ?? client).get<GetLatencyP95Responses, GetLatencyP95Errors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/stats/latency-p95",
    ...options,
  });

/**
 * Get cache hit rate percentage over time
 *
 * Returns the cache hit rate as a percentage as a time series.
 * Requires auditor or admin role.
 *
 * **Requires Prometheus**: This endpoint queries Prometheus for metrics.
 * If Prometheus is not configured, returns 501 Not Implemented.
 */
export const getCacheHitRate = <ThrowOnError extends boolean = false>(
  options?: Options<GetCacheHitRateData, ThrowOnError>,
) =>
  (options?.client ?? client).get<GetCacheHitRateResponses, GetCacheHitRateErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/stats/cache-hit-rate",
    ...options,
  });

/**
 * Get database connection pool utilization over time
 *
 * Returns the database connection pool utilization as a percentage.
 * This metric shows how many connections are active vs available.
 * Requires auditor or admin role.
 *
 * **Requires Prometheus**: This endpoint queries Prometheus for metrics.
 * If Prometheus is not configured, returns 501 Not Implemented.
 */
export const getDbPoolUtilization = <ThrowOnError extends boolean = false>(
  options?: Options<GetDbPoolUtilizationData, ThrowOnError>,
) =>
  (options?.client ?? client).get<GetDbPoolUtilizationResponses, GetDbPoolUtilizationErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/stats/db-pool-utilization",
    ...options,
  });

/**
 * Get current datastore-backed admin inventory summary
 *
 * Returns current counts for persisted conversations, entries, memories,
 * and optional outbox retention details. Requires auditor or admin role.
 *
 * This endpoint reads directly from the configured datastores. It does not
 * require Prometheus.
 */
export const getStatsSummary = <ThrowOnError extends boolean = false>(
  options?: Options<GetStatsSummaryData, ThrowOnError>,
) =>
  (options?.client ?? client).get<GetStatsSummaryResponses, GetStatsSummaryErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/stats/summary",
    ...options,
  });

/**
 * Get P95 store operation latency by operation type
 *
 * Returns the 95th percentile latency for memory store operations,
 * broken down by operation type (e.g., createConversation, appendAgentEntries).
 * Requires auditor or admin role.
 *
 * **Requires Prometheus**: This endpoint queries Prometheus for metrics.
 * If Prometheus is not configured, returns 501 Not Implemented.
 *
 * **Requires MeteredMemoryStore**: This endpoint relies on metrics from
 * MeteredMemoryStore. If the store is not instrumented, the response
 * will be empty.
 */
export const getStoreLatencyP95 = <ThrowOnError extends boolean = false>(
  options?: Options<GetStoreLatencyP95Data, ThrowOnError>,
) =>
  (options?.client ?? client).get<GetStoreLatencyP95Responses, GetStoreLatencyP95Errors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/stats/store-latency-p95",
    ...options,
  });

/**
 * Get store operation throughput by operation type
 *
 * Returns the throughput (operations per second) for memory store operations,
 * broken down by operation type (e.g., createConversation, appendAgentEntries).
 * Requires auditor or admin role.
 *
 * **Requires Prometheus**: This endpoint queries Prometheus for metrics.
 * If Prometheus is not configured, returns 501 Not Implemented.
 *
 * **Requires MeteredMemoryStore**: This endpoint relies on metrics from
 * MeteredMemoryStore. If the store is not instrumented, the response
 * will be empty.
 */
export const getStoreThroughput = <ThrowOnError extends boolean = false>(
  options?: Options<GetStoreThroughputData, ThrowOnError>,
) =>
  (options?.client ?? client).get<GetStoreThroughputResponses, GetStoreThroughputErrors, ThrowOnError>({
    security: [{ scheme: "bearer", type: "http" }],
    url: "/v1/admin/stats/store-throughput",
    ...options,
  });
