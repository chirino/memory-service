# @chirino/memory-service-vercelai

Shared Memory Service primitives for Express + Vercel AI SDK applications.

This package is consumed by local examples via a `file:` dependency today and is structured for npm publication.
