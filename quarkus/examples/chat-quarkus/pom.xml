<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.github.chirino.memory-service</groupId>
    <artifactId>memory-service-quarkus-examples-parent</artifactId>
    <version>999-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>chat-quarkus</artifactId>
  <packaging>jar</packaging>
  <name>Memory Service - Demo Chat App - Quarkus</name>

  <dependencies>

    <!-- Memory Service Extension (provides Dev Services) -->
    <dependency>
      <groupId>io.github.chirino.memory-service</groupId>
      <artifactId>memory-service-extension</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.github.chirino.memory-service</groupId>
      <artifactId>memory-service-proto-quarkus</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- Depend on shared API module for generated client / DTOs -->
    <dependency>
      <groupId>io.github.chirino.memory-service</groupId>
      <artifactId>memory-service-rest-quarkus</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- LangChain4j for the agent -->
    <dependency>
      <groupId>io.quarkiverse.langchain4j</groupId>
      <artifactId>quarkus-langchain4j-openai</artifactId>
      <version>${quarkus-langchain4j.version}</version>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-arc</artifactId>
    </dependency>

    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-grpc</artifactId>
    </dependency>

    <!-- PostgreSQL (to trigger dev services for the memory-service container) -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-jdbc-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-oidc</artifactId>
    </dependency>
    <!-- Redis (to trigger dev services for the memory-service container) -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-redis-client</artifactId>
    </dependency>
    <!-- Core Quarkus for agent app -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest</artifactId>
    </dependency>

    <!-- REST client support for generated OpenAPI clients -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest-client-jackson</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest-client-oidc-token-propagation</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest-jackson</artifactId>
    </dependency>

    <!-- Security / OIDC, for delegating calls to service -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-security</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-smallrye-health</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-smallrye-openapi</artifactId>
    </dependency>

    <!-- Testing -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-junit5</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-test-keycloak-server</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.rest-assured</groupId>
      <artifactId>rest-assured</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <profiles>
    <!--
      Quinoa profile: active in local dev (when CI env var is not set AND
      the pre-built frontend directory does not already exist).
      Both conditions are ANDed, so either one being false suppresses Quinoa:
        - CI builds: CI=true → !env.CI false → Quinoa skipped (ci-frontend profile handles it)
        - Docker builds: dist already copied into the dir → <missing> false → Quinoa skipped
        - Local dev: neither condition blocks → Quinoa activates for live-reload
    -->
    <profile>
      <id>quinoa</id>
      <activation>
        <property>
          <name>!env.CI</name>
        </property>
        <file>
          <missing>${project.basedir}/src/main/resources/META-INF/resources</missing>
        </file>
      </activation>
      <dependencies>
        <!-- UI -->
        <dependency>
          <groupId>io.quarkiverse.quinoa</groupId>
          <artifactId>quarkus-quinoa</artifactId>
          <version>${quarkus-quinoa.version}</version>
        </dependency>
      </dependencies>
    </profile>

    <!--
      CI-frontend profile: active when the CI env var is set (GitHub Actions, etc.).
      Adds the pre-built chat-frontend JAR as a dependency. The JAR packages the
      built frontend assets at META-INF/resources, so Quarkus serves them as
      static content without needing npm at all.
    -->
    <profile>
      <id>ci-frontend</id>
      <activation>
        <property>
          <name>env.CI</name>
        </property>
      </activation>
      <dependencies>
        <dependency>
          <groupId>io.github.chirino.memory-service</groupId>
          <artifactId>chat-frontend</artifactId>
          <version>${project.version}</version>
        </dependency>
      </dependencies>
    </profile>
  </profiles>

</project>
