/**
 * Convenience artifact whose Maven dependencies provide every supported Memory Service binary.
 *
 * <p>The artifact intentionally contains no native payload of its own.
 */
package io.github.chirino.memoryservice.process.binaries;
