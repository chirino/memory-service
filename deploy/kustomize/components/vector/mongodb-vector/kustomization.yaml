apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Enables MongoDB Atlas Vector Search on the memory-service.
# Requires the mongodb datastore component.

configMapGenerator:
  - name: memory-service-config
    behavior: merge
    literals:
      - MEMORY_SERVICE_VECTOR_KIND=mongo
