apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  # Gateway instance (Envoy Gateway controller + GatewayClass installed separately via Taskfile)
  - gateway.yaml
  # HTTPRoutes for services
  - httproute-memory-service.yaml
  - httproute-keycloak.yaml
  - httproute-grafana.yaml
  - httproute-prometheus.yaml
  - httproute-demo-chat.yaml
