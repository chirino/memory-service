apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: memory-service
  namespace: memory-service
spec:
  parentRefs:
    - name: memory-service-gateway
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: memory-service
          port: 8080
