apiVersion: v1
kind: Service
metadata:
  name: chat-quarkus
  labels:
    app: chat-quarkus
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: chat-quarkus
